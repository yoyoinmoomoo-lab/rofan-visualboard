# Rofan Visualboard v0.0.1 Release Notes

**Release Date:** 2024-12-19

## 🎉 첫 공식 릴리스

Rofan Visualboard (Vivid Chat)의 첫 공식 버전입니다. 이 버전은 안정성과 사용자 경험 개선에 중점을 둡니다.

## ✨ 주요 기능

- **자동 업데이트**: rofan.ai 채팅 페이지에서 AI 응답이 생성되면 자동으로 분석 및 시각화
- **수동 분석**: 사용자가 직접 "분석" 버튼을 클릭하여 현재 대화를 분석
- **시각화 보드**: 등장인물, 장면, 감정 상태를 시각적으로 표시
- **캐릭터 추적**: 대화 내 등장인물의 연속성 유지 및 추적

## 🔧 개선 사항

### Extension Context 안정화
- Extension context invalidated 발생 시 자동 업데이트가 안전하게 중지되도록 개선
- Observer 및 interval 콜백에 `chrome.runtime.id` 체크 추가
- 모든 storage/sendMessage 호출에 try/catch 및 context 체크 추가
- invalidated 감지 시 observer 자동 중지 및 사용자에게 안내 메시지 표시

### API 응답 파싱 개선
- 표준화된 API 응답 형식 (`{ ok: true, data: { state } }`) 지원
- 하위 호환성을 위한 fallback 처리 (TODO: 추후 제거 예정)
- 에러 응답 처리 개선 (`ok: false` 응답 시 보드 유지 및 에러 메시지 표시)

### 메시지 전달 안정화
- service-worker에서 `NEW_LAST_AI_TURN` 메시지 중계 시 `sendResponse` 및 `return true` 처리
- `message port closed` 에러 방지
- 한 번 실패하면 stop 정책으로 무한 에러 루프 방지

## 🐛 버그 수정

- 자동 업데이트가 Extension context invalidated로 인해 크래시하던 문제 해결
- API 응답 파싱 실패 시 앱이 크래시하던 문제 해결
- 메시지 포트가 닫혀도 observer가 계속 실행되던 문제 해결

## 📋 기술적 세부사항

### 변경된 파일
- `content.js`: Extension context 체크 및 에러 처리 강화
- `service-worker.js`: 메시지 중계 시 응답 처리 개선
- `sidepanel.js`: API 응답 파싱 로직 개선

### 호환성
- **rofan.world**: v1.2.1+ 호환
- **Chrome**: Manifest V3 지원
- **rofan.ai**: 최신 버전 호환

## 🚀 설치 방법

1. Chrome 브라우저에서 `chrome://extensions/` 접속
2. 우측 상단의 "개발자 모드" 활성화
3. "압축해제된 확장 프로그램을 로드합니다" 클릭
4. 이 프로젝트 폴더 선택

## 📝 알려진 이슈

- 자동 업데이트가 Extension context invalidated로 인해 중지되면, rofan.ai 탭을 새로고침한 후 다시 켜야 합니다.

## 🔮 다음 버전 계획

- 자동 업데이트 재시도 기능
- 에러 복구 자동화
- 성능 최적화

---

**GitHub Release**: [v0.0.1](https://github.com/yoyoinmoomoo-lab/vivid-chat/releases/tag/v0.0.1)

